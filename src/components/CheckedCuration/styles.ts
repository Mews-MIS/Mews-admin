import styled from "@emotion/styled";

export const AddBtn = styled.img`
  src: ${(props) => props.src};
  cursor: pointer;
`;

export const DeleteBtn = styled.img`
  src: ${(props) => props.src};
  cursor: pointer;
`;
